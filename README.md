# Client
After launching the server, you can connect with:

```bash
python src/client.py
```

You can then follow the instruction, inputting your username and then sending messages directly to the server with the user_request protocol message.
